var jsonData = pm.response.json();

pm.test("Status code is 400", function () {
    pm.response.to.have.status(400);
});

pm.test("Reponse body contains error fields", function () {
    pm.expect(jsonData.status).to.eql(false);
    pm.expect(jsonData.statusCode).to.eql(400);
    pm.expect(jsonData.message).to.eql('Uploaded file must be an image of 2MB or less');
});