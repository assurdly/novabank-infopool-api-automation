var jsonData = pm.response.json();

pm.test("Status code is 400", function () {
    pm.response.to.have.status(400);
});
pm.test("Reponse body contains standard success fields", function () {
    pm.expect(jsonData.statusCode).to.eql(400);
    pm.expect(jsonData.status).to.eql(false);
    pm.expect(jsonData.message).to.eql('Cannot create organization without parent organization');
});

pm.environment.set("test_organization_id", jsonData.response.id)